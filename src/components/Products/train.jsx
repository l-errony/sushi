/*
import React from 'react';
import s from "./Products.module.css";
import Cart from "../Cart/Cart";

const Products = ({products, categories}) => {



    /!*const currentCat = ''
    const all = products.map((item, i) => {

        if (i !== 0 && currentCat !== item.cat){
            </div>
        }


        if(currentCat !== item.cat){
            <div className={s.contentItems}>
        }
                <Cart/>


    })

</div>*!/


    /!*const byCats = {};

    const all = products.map((item, i) => {
        if(byCats.indexOf(item.cat) === -1){
            byCats[item.cat] = [];
        }
        byCats[item.cat].push(item);
    })

    const allByCats = byCats.map((item, i) => {
        <div className={s.contentItems}>
                products.map(({name}) =>
            <Cart name={name}/>
            ) })
        </div>
    })*!/


    /!*const allByCats = categories.map((item, i) => {
        <div className={s.contentItems}>
            products.map((key, j) =>
                if(key.cat === item)
                    <Cart name={name}/>
            ) })
        </div>
    })


    const sushi = {}
    const rolls = categories.map((item, i) => {
        sushi.item
    })
    console.log(sushi)


    return (
        <div className={s.container}>
            {/!*<div className={s.contentItems}>
                {
                    products.map(({name}) =>
                        <Cart name={name}/>
                    )
                }
            </div>
        </div>
    );
};

export default Products;
    */
